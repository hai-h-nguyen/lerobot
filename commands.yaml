# training
python3 scripts/train.py --dataset.repo_id=hainh22/multi-modal-peg-in-square-hole --policy.type=diffusion --job_name=diffusion_peg_in_square_hole --wandb.enable=true --output_dir=output/peg-in-square-hole-test


# peg in hole ft only
python3 lerobot/scripts/train.py --config train_configs/diffusion/configs_no_ft.json